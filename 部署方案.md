# Codex 便携平台 — 部署方案

> 目标：把本方案复制到另一台电脑，照着就能把整个平台复原出来。

## 一、项目是什么

Codex CLI + DeepSeek 线上 API + Ollama 本地模型 + Open WebUI 聊天界面的便携组合。
所有配置和数据都在项目文件夹内，整包复制即迁移。详细介绍见 `README.md`。

## 二、外部依赖（无法随文件夹打包，需每台电脑单独装）

| 依赖 | 用途 | 安装 |
|------|------|------|
| Node.js v18+ | 运行 codex-bridge 与 Codex CLI | https://nodejs.org |
| Docker Desktop | 运行 Ollama / Open WebUI 容器 | https://www.docker.com |
| DeepSeek API Key | 线上模型调用 | https://platform.deepseek.com |

## 三、从零部署步骤

1. 复制整个 `codex-portable` 文件夹到目标电脑（建议放盘根，如 `E:\codex-portable`）。
2. 安装并启动 Docker Desktop，等它显示 "Engine running"。
3. 安装 Node.js v18+，重新打开终端。
4. 进入项目目录，运行 `import.bat`：
   - 检查 Node.js / npm / Docker 是否就绪
   - 导入 `ollama-image.tar` 和 `open-webui-image.tar`（已导入会跳过；没有 tar 则启动时联网拉取）
   - 写入 `CODEX_HOME` 用户环境变量（指向本文件夹 `config`）
5. 运行 `setup.bat`：安装 Codex CLI 并初始化。
6. 运行 `启动.bat`（等于 `start.bat`：Ollama + bridge + Open WebUI）。
7. 验证（见第九节）。

## 四、日常启停

| 操作 | 命令 |
|------|------|
| 启动全部 | `启动.bat` |
| 停止全部 | `关闭.bat` |
| 查状态 | `状态.bat` |
| 只起线上代理 | `start.bat deepseek` |
| 只起本地模型 | `start.bat ollama` |
| 只起聊天界面 | `start.bat webui` |

## 五、每台电脑需要单独配置的内容

1. `codex-bridge\.env`：填真实 `DEEPSEEK_API_KEY=`（给别人用前务必更换，勿外传真实密钥）。
2. Open WebUI 管理后台手动添加一次（设置 → 外部连接）：
   - API 地址：`http://host.docker.internal:4000/v1`
   - API 密钥：`sk-proxy-local-codex-portable-key-2026`
3. 若使用需要登录授权的供应商配置，首次运行 `codex` 时按提示完成授权。

## 六、数据位置（迁移时一起带走）

| 数据 | 位置 |
|------|------|
| Ollama 模型 | `projects\ollama\data` |
| Open WebUI 数据库/上传 | `projects\open-webui\data` |
| Codex 配置/会话/日志 | `config\` |
| 离线镜像包 | `ollama-image.tar`、`open-webui-image.tar`（可选） |

## 七、给别人用之前的安全处理

- 删除 `config\auth.json`、`config\.sandbox-secrets\` 等本机凭据文件
- 更换 `codex-bridge\.env` 里的 API Key
- 不需要离线模型时可删两个 `.tar` 大文件省空间

## 八、故障排查

| 现象 | 处理 |
|------|------|
| codex 不是内部或外部命令 | 运行 `setup.bat` 后重开终端 |
| 镜像导入失败 | 确认 Docker Desktop 已启动，再跑 `import.bat` |
| 容器名 ollama 冲突 | 别的项目占了同名容器，先停掉/移除那个容器 |
| 端口 3002/4000/11434 被占用 | 关掉占用程序后再启动 |
| 换机后网页空白 | 清浏览器缓存，重跑 `import.bat` + `启动.bat` |

## 九、验证清单

- [ ] `状态.bat` 三个服务均显示"运行中"
- [ ] http://localhost:3002 能打开聊天界面
- [ ] `codex.bat` 能正常对话