# 项目日志（codex-portable）

> 记录项目状态与每次改动；开始工作前先读这里。最新条目在最上面。

## 2026-08-14 整理：独立目录 + 文档/脚本完善

- 从 `E:\shengyinkelongshuzirendamoxing\`（数字人大模型平台项目内）迁出，独立到 `E:\codex-portable`。
- 文档中残留的 D:\ 旧路径全部更新为当前 E:\codex-portable 路径（README / HOW_TO_START / STARTUP / config 注释 / deepseek.config.toml）。
- README.md 重写为完整项目介绍（组件、快速开始、一键脚本、模型切换、目录结构、FAQ）。
- 新增中文一键脚本：`启动.bat`、`关闭.bat`、`状态.bat`。
- 新增 `部署方案.md`、`项目日志.md`、`tests\冒烟测试.bat`、`ziliao\说明.md`。
- `codex.bat` / `codex.ps1` 增加回退：优先用 PATH 里的 codex，找不到再走本机 WorkBuddy 绝对路径。

## 待办 / 遗留

- Open WebUI 的 DeepSeek 外部连接需在新环境手动添加一次（见部署方案第五节）。
- `codex-bridge\.env` 含真实 API Key，换人使用前务必更换。
- `config\` 下历史运行数据（history.jsonl、logs_*.sqlite、cap_sid）里残留 D:\ 旧路径记录，属历史数据，不影响使用，未改动。